# Architecture

Layers: baseline decoder transformer, MLA-lite attention, recurrent latent refinement, tiny MoE, verifier/search shell. Every layer must justify its complexity against baseline measurements.
